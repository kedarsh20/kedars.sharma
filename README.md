# Academic homepage (GitHub Pages)

This repository hosts my academic website using GitHub Pages + Jekyll (theme: minima).

## How to update
- Edit Markdown files (`*.md`) and push to `main`.
- Upload PDFs/images to `assets/`.

## Local preview (optional)
If you have Ruby/Jekyll installed:
```bash
bundle exec jekyll serve
